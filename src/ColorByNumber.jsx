import ColorPixel from "./ColorPixel"
import { PNG } from "pngjs"

const ColorGrid = (image) => {
  return (
    <div style={{ width: '16px', height: '16px', border: `2px solid ${col}`, background:'rgb(16,16,16'}}>
      <p style={{ fontSize: '8pt'}}>num</p>
    </div>
  )
}